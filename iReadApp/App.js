import React from 'react';
import {SafeAreaView, ScrollView, Text} from 'react-native';

const App = () => {
  return (
    <SafeAreaView>
      <ScrollView>
        <Text>Hello word</Text>
      </ScrollView>
    </SafeAreaView>
  );
};

export default App;
